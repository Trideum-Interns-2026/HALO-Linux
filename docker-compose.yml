services:
  px4-sitl:
    build:
      context: .
    image: px4-sitl-dev:latest
    privileged: true
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: ["MicroXRCEAgent", "udp4", "-p", "8888"]